version: 1
applications:
  - appRoot: sandbox
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
    backend:
      phases:
        preBuild:
            commands:
            - echo "preBuild of backend"
        build:
            commands:
            - echo "build of backend"
        postBuild:
            commands:
            - echo "postBuild of backend"
    
